# cryptkit
CLI tool that identifies and solves common CTF crypto and stego challenges. Detects encoding type, chains the right solvers, and shows you what it found — instead of making you guess which tool to run first.
